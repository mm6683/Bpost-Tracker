name = "bpost-tracker"
main = "src/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Static assets served from the public directory.
# The Worker handles /proxy; everything else falls through to these files.
[assets]
directory = "./public"
